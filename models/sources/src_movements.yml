version: 2

sources:
  - name: stg_source
    schema: "{{ env_var('DBT_SOURCE_SCHEMA', 'stg') }}"
    tables:
      - name: stg_fact_staff_movements
        description: "Стейджинговая таблица проходов сотрудников, заполненная внешним пайплайном."
        columns:
          - name: id
            tests:
              - not_null
          - name: staff_id
            tests:
              - not_null
          - name: dt
            tests:
              - not_null
          - name: created_at
            tests:
              - not_null
