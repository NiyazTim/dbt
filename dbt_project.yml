name: "staff_movements_dbt"
version: "1.0.0"
config-version: 2

profile: "staff_movements_dbt"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  staff_movements_dbt:
    +materialized: table
